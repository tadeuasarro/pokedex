const pokemonInitial = {
  results: [],
};

const pokemonReducer = (state = pokemonInitial, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default pokemonReducer;
